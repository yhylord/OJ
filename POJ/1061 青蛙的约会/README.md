POJ 1061 青蛙的约会

【题目大意】

两只青蛙在同一条长度为L的纬线上，青蛙A从坐标x出发步长为m，青蛙B从坐标y出发步长为n，问碰面需要的最少跳跃次数或判断无解。

【算法分析】

设碰面需要的跳跃次数为p，则x + mp = y + mp(mod L)，即x + mp + Lq = y + np，即(m - n)p + Lq = y - x。

这就转化为求ax + by = c这个二元一次不定方程的最小的x了，用扩展欧几里得算法即可。时间复杂度O(logN)。
