POJ 2396 Budget

【题目大意】

给出一个N * M的矩阵(1 <= N <= 200, 1 <= M <= 20)的每行和每列的和，并给出对于每个格子大于/小于/等于某值的限制，求可行方案。

【算法分析】

初步的想法是，从源点向各行连上下界均为该行和的边，从各列向汇点连上下界均为该列和的边，从各行向各列连对应各点的上下界的边，求有源汇的可行流即可。

考虑到，每个格子都只能连一条边，而且题目给出的关系可能本身就有矛盾，因此我们必须把读入的大小关系先处理为任意点的上下界，其中注意判断矛盾，然后再进行加边。时间复杂度O(NM(N + M)^2)。
