BZOJ 2768 冠军调查

分割成两个集合且代价最小是经典的最小割问题。暂时不考虑集合间边的代价，仅仅分配每个点所在的集合，我们将源点向集合B中的点连容量为1的边，将集合A中的点向汇点连容量为1的边，那么要进入相反的集合，必然要割掉与源汇相连的那条边，从而对应一种分配方案，T集合即A集合，S集合即B集合。

同时，我们可以发现，在同一个集合里的点之间是不需要割开的，故不可能存在割边的。那么我们将原图中的无向关系边正向反向均加入新图中，不在同一集合里的两点间必须要割掉此边。由此，新图的一个割就对应了一种合法方案的代价，最小割即为最小代价。时间复杂度O(MN^2)。
