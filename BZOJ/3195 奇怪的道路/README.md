BZOJ 3195 奇怪的道路

用f(i, j, s, c)表示处理到第i个点，共用j条边，(i - k) ~ i这些点度的奇偶性为二进制数s，当前处理的连边为(i, i - k + c)的方案数。

对于c < k，若连边，则可以转移到f(i, j + 1, s', k)；若不连边，则可以转移到f(i, j, s, k + 1)。

对于c = k，只有当i - k的度为偶数才可以转移到f(i + 1, j, s', 0)。

时间复杂度O(NMK * 2^K)。
