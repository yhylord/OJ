BZOJ 2408 混乱的置换

首先我们发现，已知了最后一列，那么由于矩阵字典序最小第一列也就确定了（最后一列排个序即可），那么由第一列接在最后一列后面我们再排序可以得到前两列，以此类推我们可以得到整个矩阵，但是复杂度过高。

考虑每次的排序其实是个双关键字排序，第一关键字是确定的（最后一列），第二关键字其实也是确定的（字典序最小，因此就是行号），那么我们只要进行一次双关键字排序就可以知道每次当前末尾字符会转移到哪里，重复模拟即得答案，时间复杂度O(N^2)。
