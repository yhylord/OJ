BZOJ 1492 货币兑换

用f(i)表示到第i天结束时能够拥有的最多的钱，同时我们也可以计算出ca_i与cb_i表示用这些钱在当天能够买入的A券和B券数量。

那么f(i) = max{f(i - 1), ca_j * a_i + cb_j * b_i}(0 <= j < i)。

令x = ca_j, y = cb_j, a = a_i, b = b_i，我们要max z = ax + by，也即要使直线y = -a / b * x + z / b的纵截距最大。

由于x和斜率-a / b均不满足单调性，我们可以用对时间分治的方法来完成斜率优化。时间复杂度O(NlogN)。
