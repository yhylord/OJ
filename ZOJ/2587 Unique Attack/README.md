ZOJ 2587 Unique Attack

【题目大意】

给出一个N个点(2 <= N <= 800)M条边(1 <= 10000)的连通无向图和源点A汇点B，判断使得A、B不连通的最小割是否唯一。

【算法分析】

我们发现，若一个最小割唯一，当且仅当做完最大流以后，残留网络中的S能到达的点的集合和能到达T的点的集合的并集为V。

因此，我们只要在最大流结束后在残余网络中对S进行BFS，并将图反向后对T进行BFS判断并集即可。注意，对残余网络建反向图非常简单，只要将每条边及其反向边可以流的量交换即可。时间复杂度O(MN^2)。
