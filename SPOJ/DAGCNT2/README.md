SPOJ DAGCNT2

【题目大意】

给出一个N(1 <= N <= 20000)个点，M(1 <= M <= 500000)条边的有向无环图，每个点有一个权值，求每个点能到达的点的权值和。

【算法分析】

我们对原图进行拓扑排序，按其逆序进行状压DP。记f[i]表示i能到达的点的集合（32位一压），每次将它指出去的点的状态or起来就可以了。

此题略卡常数，注意加一些优化，首先注意不要每组测试数据对整个DP数组memset，因为小数据里有40组。

其次or的过程中，假如指出去的某个点已经在你or的结果里就不要再去or它的6状态了。

时间复杂度O(N^2 + NM / 32)。
