SPOJ MTREE

【题目大意】

给出一棵N(2 <= N <= 100000)个结点的树，定义路径的权值为路径所有边权的乘积，求树上所有路径的权值和。

【算法分析】

树上路径统计的问题，很容易借鉴树的点分治的思路。

考虑过根路径的答案，因为这东西统计比较简单，对于每个根并不需要分别处理。

记f[i]表示以i为起点的路径的权值和，直接从下往上一个个推就可以了。

求答案的话，每次枚举到一个儿子的时候，通过乘之前的儿子就可以了。

时间复杂度O(N)。
