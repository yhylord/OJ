SGU 194 Reactor Cooling

【题目大意】

给出一个N个点(1 <= N <= 200)M条边的有向图，每条边的容量有上下界，求满足流量平衡条件的原图是否存在一个可行流。

【算法分析】

无源汇的可行流问题，直接添加新源点S和新汇点T，对于原图中的边<u, v>（下界a, 上界b），转化为新图中的边<S, v>（容量a），<u, T>（容量a），<u, v>（容量b - a），如新图中最大流等于原图中下界和则有解，输出方案即可。时间复杂度O(MN^2)。
