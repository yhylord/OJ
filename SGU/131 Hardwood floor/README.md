SGU 131 Hardwood floor

【题目大意】

给出一个N * M(1 <= N, M <= 9)的棋盘，用1 * 2的矩形骨牌和L形的骨牌不重叠地覆盖，求覆盖满的方案数。

【算法分析】

我们用f[i][j]表示前i - 1行覆盖满，第i行状态为j（1表示此列已填，0表示留给下一行填上来）的方案数。

转移比较复杂，因此我们用DFS来得到所有合法的转移关系，对于某一格有7种摆放方式，注意细节即可。

设转移数为S，经试验在M = 9时S = 79248，时间复杂度为O(NS)。
